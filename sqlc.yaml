version: "2"
sql:
  - engine: "postgresql"
    queries: "sql/queries/"
    schema: "migrations/"
    gen:
      go:
        package: "db"
        out: "internal/shared/database/db"
        sql_package: "pgx/v5"
        emit_interface: true
        emit_json_tags: true

